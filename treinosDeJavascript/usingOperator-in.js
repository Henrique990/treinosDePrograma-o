{

    let arr = [
        'redwood', 'bay', 'cedar', 'oak', 'maple'
    ]
    
    // exemplo do operador in
    if ('maple' in arr) {
        console.log('maple is in the array')
    } else {
        console.log('maple is not in the array')
    }
}

{
    let arr = [
        'redwood', 'bay', 'cedar', 'oak', 'maple'
    ]

    // exemplo do operador in
    console.log('maple' in arr) // true
}